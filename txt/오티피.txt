시계 + key 값 : hash함수가 들어있다. 6자리수
숫자는 1분마다 값이 바뀌게 된다.
물리적으로는 해킹을 할 수가 없다.
서버와 기계에 있는 시간이 같다.
학생계좌 뒤에 시리얼값을 넣어준다.
완벽한 오티피는 물리적장치가 있어야 한다.

------------------------------------
학생 입학하면 특정값(pin번호)을 배정해서 디비에 저장한다.
포레스트에 접속하면 오티피값이 뜨게 한다. 
학번 핀값 비밀핀값 활성화여부 발급일자
핀값을 기반으로해서 현재서버시간을 계산해서 특정값을 만들어낸다.
앞에 4자리가 실제 핀값이다.
뒤 6자리수가 시간( 	1분값만 유효한 값)

Open API 가 열려있다.
파라미터 2개를 받는다. 6자리수를 2개로 나누는데, 앞에 2개로 p1은 핀값, 뒤4개는 p2로 OTP값
확인 누르는순간 json값으로 저사이트에서 성공, 실패값이 넘어온다. 끝나기 직전에 누르면 false로 뜰수가 있다.
요청이 몇회이상 들어오면 막아야하지만 아직 구현은 못함.

실패: error : E010
성공 : sts Y
개발자는 ajax로 요청하고 돌아온 객체를 json로 파싱을 알아서 해줘서 if문

교직원 오티피잇나요??? 있다.
교수도 있다.

단 쓸거면 서버IP와 사용기간을 전자계산소에 알려줘야한다.
도메인보다는 서버IP



-------------
여담 : oauth2